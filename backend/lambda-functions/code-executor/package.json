{
  "name": "code-executor",
  "version": "1.0.0",
  "description": "Lambda function for executing code in sandboxed Docker containers",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rm -rf dist",
    "deploy": "npm run build && zip -r function.zip dist/ node_modules/ && aws lambda update-function-code --function-name code-executor --zip-file fileb://function.zip",
    "test": "jest",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts"
  },
  "dependencies": {
    "aws-sdk": "^2.1400.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/jest": "^29.5.0",
    "@types/node": "^20.19.13",
    "@types/uuid": "^9.0.8",
    "jest": "^29.7.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.2"
  }
}
